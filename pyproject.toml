[project]
name = "anc-prototype"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["src", "."]
include = ["anc*", "experiments*"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
